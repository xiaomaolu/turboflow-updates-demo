{
  "name": "turboflow-updates-demo",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free static generator for the proposed TurboFlow Updates section.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "preview": "node scripts/serve.mjs",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
